<!DOCTYPE html>
<html>
<head>
    <title>Web Recon Report</title>
    <style>
        body { font-family: Arial; margin: 40px; }
        h2 { border-bottom: 1px solid #ccc; }
        .high { color: red; }
        .medium { color: orange; }
        .low { color: green; }
    </style>
</head>
<body>

<h1>Web Reconnaissance Report</h1>
<p><strong>Target:</strong> {{ data.domain }}</p>
<p><strong>Score:</strong> {{ data.score }}/100</p>

<h2>Subdomains</h2>
<ul>
{% for sub in data.subdomains %}
    <li>{{ sub.subdomain }} — {{ "Alive" if sub.alive else "Not reachable" }}</li>
{% endfor %}
</ul>

<h2>Findings</h2>
<ul>
{% for f in data.findings %}
    <li class="{{ f.severity|lower }}">
        <strong>{{ f.severity }}</strong>: {{ f.issue }}<br>
        Recommendation: {{ f.recommendation }}
    </li>
{% endfor %}
</ul>

</body>
</html>

